name: build-weather-ios
on:
  pull_request:
    branches:
      - "develop"
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: bundle exec fastlane tests
